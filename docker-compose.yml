services:
  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    volumes:
      - ./app:/usr/src/app
  db:
    build:
      context: .
      dockerfile: db/Dockerfile